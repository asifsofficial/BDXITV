<?php
$channels = [
    ['id'=>1,  'name'=>'T Sports',              'cat'=>'Sports',  'logo'=>'T-Sports.png'],
    ['id'=>2,  'name'=>'Star Sports 1 HD',      'cat'=>'Sports',  'logo'=>'Star-Sports-1-HD.png'],
    ['id'=>3,  'name'=>'Star Sports 2 HD',      'cat'=>'Sports',  'logo'=>'Star-Sports-2-HD.png'],
    ['id'=>16, 'name'=>'Star Sports Select 2',  'cat'=>'Sports',  'logo'=>'Star-Sports-Select-2.png'],
    ['id'=>28, 'name'=>'Star Select 1 HD',      'cat'=>'Sports',  'logo'=>'Star-Select-1-HD.png'],
    ['id'=>29, 'name'=>'Sony Sports 2 HD',      'cat'=>'Sports',  'logo'=>'Sony-Sports-2-HD.png'],
    ['id'=>30, 'name'=>'Sony Sports 3',         'cat'=>'Sports',  'logo'=>'Sony-Sports-3.png'],
    ['id'=>31, 'name'=>'Star Sports 3',         'cat'=>'Sports',  'logo'=>'Star-Sports-3.png'],
    ['id'=>40, 'name'=>'Eurosports HD',         'cat'=>'Sports',  'logo'=>'Eurosports-HD.png'],
    ['id'=>66, 'name'=>'Golf Sports',           'cat'=>'Sports',  'logo'=>'Golf-Sports.png'],
    ['id'=>72, 'name'=>'Sony Sports 4',         'cat'=>'Sports',  'logo'=>'Sony-Sports-4.png'],
    ['id'=>74, 'name'=>'Sony Sports 1 HD',      'cat'=>'Sports',  'logo'=>'Sony-Sports-1-HD.png'],
    ['id'=>87, 'name'=>'Fast Sports HD',        'cat'=>'Sports',  'logo'=>'Fast-Sports-HD.png'],
    ['id'=>88, 'name'=>'A Sports HD',           'cat'=>'Sports',  'logo'=>'A-Sports-HD.png'],
    ['id'=>92, 'name'=>'PTV Sports HD',         'cat'=>'Sports',  'logo'=>'PTV-Sports-HD.png'],
    ['id'=>101,'name'=>'Sony Sports 5 HD',      'cat'=>'Sports',  'logo'=>'Sony-Sports-5-HD.png'],
    ['id'=>104,'name'=>'Star Sports 4K',        'cat'=>'Sports',  'logo'=>'Star-Sports-4k.png'],
    ['id'=>105,'name'=>'Ten Cricket',           'cat'=>'Sports',  'logo'=>'Ten-Cricket.png'],
    ['id'=>12, 'name'=>'Colors Bangla HD',      'cat'=>'Bangla',  'logo'=>'COLORS BANGLA HD1745033568'],
    ['id'=>14, 'name'=>'Zee Bangla HD',         'cat'=>'Bangla',  'logo'=>'ZEE BANGLA HD1745033633'],
    ['id'=>17, 'name'=>'Sun Bangla HD',         'cat'=>'Bangla',  'logo'=>'SUN BANGLA HD1745033791'],
    ['id'=>18, 'name'=>'Star Jalsha HD',        'cat'=>'Bangla',  'logo'=>'STAR JALSHA HD1745033853'],
    ['id'=>19, 'name'=>'Jalsha Movies HD',      'cat'=>'Bangla',  'logo'=>'JALSHA MOVIES HD1745033908'],
    ['id'=>43, 'name'=>'Zee Bangla Cinema',     'cat'=>'Bangla',  'logo'=>'ZEE BANGLA CINEMA1745040996'],
    ['id'=>47, 'name'=>'Colors Bangla Cinema',  'cat'=>'Bangla',  'logo'=>'COLORS BANGLA CINEMA1745041098'],
    ['id'=>54, 'name'=>'Nagorik TV',            'cat'=>'Bangla',  'logo'=>'NAGORIK1745042788'],
    ['id'=>56, 'name'=>'GTV',                   'cat'=>'Bangla',  'logo'=>'GTV1745042836'],
    ['id'=>68, 'name'=>'Duranta TV',            'cat'=>'Bangla',  'logo'=>'DURANTA TV1745043219'],
    ['id'=>76, 'name'=>'Jamuna TV',             'cat'=>'Bangla',  'logo'=>'JAMUNA TV1745043487'],
    ['id'=>77, 'name'=>'Independent TV',        'cat'=>'Bangla',  'logo'=>'INDEPENDENT TV1745043533'],
    ['id'=>78, 'name'=>'Maasranga HD',          'cat'=>'Bangla',  'logo'=>'MAASRANGA HD1745043579'],
    ['id'=>79, 'name'=>'Somoy TV HD',           'cat'=>'Bangla',  'logo'=>'SOMOY TV1745043625'],
    ['id'=>80, 'name'=>'ATN Bangla HD',         'cat'=>'Bangla',  'logo'=>'ATN BANGLA1745043671'],
    ['id'=>81, 'name'=>'Channel I HD',          'cat'=>'Bangla',  'logo'=>'CHANNEL I1745043717'],
    ['id'=>82, 'name'=>'Channel 24 HD',         'cat'=>'Bangla',  'logo'=>'CHANNEL 241745043763'],
    ['id'=>84, 'name'=>'Enterr 10 Bangla',      'cat'=>'Bangla',  'logo'=>'ENTERR 10 BANGLA1745043905'],
    ['id'=>95, 'name'=>'ATN News HD',           'cat'=>'Bangla',  'logo'=>'ATN NEWS1745044037'],
    ['id'=>109,'name'=>'BTV HD',                'cat'=>'Bangla',  'logo'=>'BTV HD1745044225'],
    ['id'=>113,'name'=>'DBC News HD',           'cat'=>'Bangla',  'logo'=>'DBC NEWS1745044271'],
    ['id'=>114,'name'=>'Ekattor TV HD',         'cat'=>'Bangla',  'logo'=>'EKATTOR TV1745044317'],
    ['id'=>116,'name'=>'News 24 HD',            'cat'=>'Bangla',  'logo'=>'NEWS 241745044363'],
    ['id'=>120,'name'=>'Sangeet Bangla',        'cat'=>'Bangla',  'logo'=>'SANGEET BANGLA1745044455'],
    ['id'=>122,'name'=>'Asiah TV',              'cat'=>'Bangla',  'logo'=>'ASIAH TV1745044501'],
    ['id'=>124,'name'=>'Global TV',             'cat'=>'Bangla',  'logo'=>'GLOBAL TV1745044547'],
    ['id'=>4,  'name'=>'Colors HD',             'cat'=>'Hindi',   'logo'=>'COLORS HD1745033382'],
    ['id'=>5,  'name'=>'And Pictures HD',       'cat'=>'Hindi',   'logo'=>'AND PICTURES HD1745033427'],
    ['id'=>7,  'name'=>'Zee TV HD',             'cat'=>'Hindi',   'logo'=>'ZEE TV HD1745033383'],
    ['id'=>10, 'name'=>'Colors Cineplex HD',    'cat'=>'Hindi',   'logo'=>'COLORS CINEPLEX HD1745033519'],
    ['id'=>11, 'name'=>'Sony Ent HD',           'cat'=>'Hindi',   'logo'=>'SONY ENT HD1745033551'],
    ['id'=>13, 'name'=>'Zee Cinema HD',         'cat'=>'Hindi',   'logo'=>'ZEE CINEMA HD1745033602'],
    ['id'=>20, 'name'=>'Star Gold HD',          'cat'=>'Hindi',   'logo'=>'STAR GOLD HD1745033962'],
    ['id'=>21, 'name'=>'Sony Max HD',           'cat'=>'Hindi',   'logo'=>'SONY MAX HD1745034018'],
    ['id'=>24, 'name'=>'Star Plus HD',          'cat'=>'Hindi',   'logo'=>'STAR PLUS HD1745038577'],
    ['id'=>42, 'name'=>'Zoom',                  'cat'=>'Hindi',   'logo'=>'ZOOM1745040961'],
    ['id'=>44, 'name'=>'Sony Aat',              'cat'=>'Hindi',   'logo'=>'SONY AAT1745041047'],
    ['id'=>53, 'name'=>'Zing',                  'cat'=>'Hindi',   'logo'=>'ZING1745042718'],
    ['id'=>69, 'name'=>'Star Gold 2 HD',        'cat'=>'Hindi',   'logo'=>'STAR GOLD 2 HD1745043265'],
    ['id'=>71, 'name'=>'Star Gold Select HD',   'cat'=>'Hindi',   'logo'=>'STAR GOLD SELECT HD1745043311'],
    ['id'=>89, 'name'=>'Hum TV',                'cat'=>'Hindi',   'logo'=>'HUM TV1745043903'],
    ['id'=>91, 'name'=>'Lotus TV HD',           'cat'=>'Hindi',   'logo'=>'LOTUS TV1745043950'],
    ['id'=>100,'name'=>'Star Bharat HD',        'cat'=>'Hindi',   'logo'=>'STAR BHARAT HD1745044030'],
    ['id'=>106,'name'=>'Max 2',                 'cat'=>'Hindi',   'logo'=>'MAX 21745044184'],
    ['id'=>107,'name'=>'Max 1',                 'cat'=>'Hindi',   'logo'=>'MAX 11745044207'],
    ['id'=>115,'name'=>'9XM Music',             'cat'=>'Hindi',   'logo'=>'9XM MUSIC1745044339'],
    ['id'=>118,'name'=>'Music India',           'cat'=>'Hindi',   'logo'=>'MUSIC INDIA1745044409'],
    ['id'=>119,'name'=>'MTV India',             'cat'=>'Hindi',   'logo'=>'MTV INDIA1745044431'],
    ['id'=>6,  'name'=>'Sony BBC Earth HD',     'cat'=>'English', 'logo'=>'SONY BBC EARTH HD1745033467'],
    ['id'=>9,  'name'=>'TLC HD',                'cat'=>'English', 'logo'=>'TLC HD1745033484'],
    ['id'=>15, 'name'=>'History TV HD',         'cat'=>'English', 'logo'=>'HISTORY TV HD1745033685'],
    ['id'=>22, 'name'=>'Discovery HD',          'cat'=>'English', 'logo'=>'DISCOVERY HD1745038501'],
    ['id'=>23, 'name'=>'NatGeo Wild HD',        'cat'=>'English', 'logo'=>'NATGEO WILD HD1745038537'],
    ['id'=>25, 'name'=>'NatGeo HD',             'cat'=>'English', 'logo'=>'NATGEO HD1745038619'],
    ['id'=>26, 'name'=>'Animal Planet HD',      'cat'=>'English', 'logo'=>'ANIMAL PLANET HD1745038659'],
    ['id'=>27, 'name'=>'Star Movies Select',    'cat'=>'English', 'logo'=>'STAR MOVIES SELECT HD1745038723'],
    ['id'=>32, 'name'=>'Sony Pix HD',           'cat'=>'English', 'logo'=>'SONY PIX HD1745039947'],
    ['id'=>33, 'name'=>'MNX HD',                'cat'=>'English', 'logo'=>'MNX HD1745039990'],
    ['id'=>34, 'name'=>'Romedy Now',            'cat'=>'English', 'logo'=>'ROMEDY NOW1745040033'],
    ['id'=>36, 'name'=>'MN Plus',               'cat'=>'English', 'logo'=>'MN PLUS1745040079'],
    ['id'=>37, 'name'=>'Unite8 Sports 2',       'cat'=>'English', 'logo'=>'ZEE CAFE HD1745040102'],
    ['id'=>38, 'name'=>'Star Movies HD',        'cat'=>'English', 'logo'=>'STAR MOVIES HD1745040148'],
    ['id'=>39, 'name'=>'Movies Now HD',         'cat'=>'English', 'logo'=>'MOVIES NOW HD1745040194'],
    ['id'=>55, 'name'=>'BBC News',              'cat'=>'English', 'logo'=>'BBC NEWS1745042858'],
    ['id'=>61, 'name'=>'And Xplore',            'cat'=>'English', 'logo'=>'AND XPLORE1745043087'],
    ['id'=>62, 'name'=>'Travel XP',             'cat'=>'English', 'logo'=>'TRAVEL XP1745043118'],
    ['id'=>67, 'name'=>'Love Nature HD',        'cat'=>'English', 'logo'=>'LOVE NATURE HD1745043195'],
    ['id'=>85, 'name'=>'AlJazeera HD',          'cat'=>'English', 'logo'=>'ALJAZEERA HD1745043833'],
    ['id'=>90, 'name'=>'Geo News HD',           'cat'=>'English', 'logo'=>'GEO NEWS HD1745043926'],
    ['id'=>97, 'name'=>'AXN HD',                'cat'=>'English', 'logo'=>'AXN HD1745044053'],
    ['id'=>102,'name'=>'BBC Earth HD',          'cat'=>'English', 'logo'=>'BBC EARTH HD1745044098'],
    ['id'=>108,'name'=>'Fashion TV HD',         'cat'=>'English', 'logo'=>'FASHIONTV HD1745044228'],
    ['id'=>8,  'name'=>'Cartoon Network',       'cat'=>'Kids',    'logo'=>'Cartoon-Network.png'],
    ['id'=>45, 'name'=>'Sonic',                 'cat'=>'Kids',    'logo'=>'Sonic.png'],
    ['id'=>46, 'name'=>'Nick',                  'cat'=>'Kids',    'logo'=>'Nick.png'],
    ['id'=>48, 'name'=>'Pogo',                  'cat'=>'Kids',    'logo'=>'Pogo.png'],
    ['id'=>49, 'name'=>'Hungama',               'cat'=>'Kids',    'logo'=>'Hungama.png'],
    ['id'=>50, 'name'=>'Bal Bharat',            'cat'=>'Kids',    'logo'=>'BAL BHARAT1745041328'],
    ['id'=>51, 'name'=>'Nick Jr',               'cat'=>'Kids',    'logo'=>'NICK JR1745041374'],
    ['id'=>75, 'name'=>'Super Hungama',         'cat'=>'Kids',    'logo'=>'SUPER HUNGAMA1745044022'],
    ['id'=>93, 'name'=>'Sony Yay',              'cat'=>'Kids',    'logo'=>'SONY YAY1745044009'],
    ['id'=>121,'name'=>'Disney Junior',         'cat'=>'Kids',    'logo'=>'DISNEY JUNIAR1745044475'],
    ['id'=>83, 'name'=>'Madani TV HD',          'cat'=>'Islamic', 'logo'=>'Madani-TV.png'],
    ['id'=>86, 'name'=>'Peace TV Bangla',       'cat'=>'Islamic', 'logo'=>'Peace-TV.png'],
    ['id'=>'f1',  'name'=>'beIN Sports',      'cat'=>'Sports',  'logo'=>'https://fwatch.tv/assets/channel-img/b8c15da0b2_image.png', 'url'=>'proxy.php?proxy_url=' . urlencode(base64_encode('https://andro.evrenesoglu57.click/checklist/androstreamliveexn4.m3u8')) . '&m3u8=1'],
    ['id'=>'f2',  'name'=>'CCTV CHINA - HD',  'cat'=>'Sports',  'logo'=>'https://fwatch.tv/assets/channel-img/1f2f08c3c5_china-central-television-logo-png_seeklogo-468614.png', 'url'=>'proxy.php?proxy_url=' . urlencode(base64_encode('https://live01-cn-ali.bvjicd.com/live/74501104.m3u8')) . '&m3u8=1'],
    ['id'=>'f3',  'name'=>'DSports',          'cat'=>'Sports',  'logo'=>'https://fwatch.tv/assets/channel-img/d4dbb56093_image.png', 'url'=>'proxy.php?proxy_url=' . urlencode(base64_encode('https://1nyaler.streamhostingcdn.top/stream/106/index.m3u8')) . '&m3u8=1'],
    ['id'=>'f4',  'name'=>'ELTA',             'cat'=>'Sports',  'logo'=>'https://fwatch.tv/assets/channel-img/d2887f62be_image.png', 'url'=>'proxy.php?proxy_url=' . urlencode(base64_encode('https://www.rtmpcdn.com/live/elta.m3u8')) . '&m3u8=1'],
    ['id'=>'f5',  'name'=>'Servus TV',        'cat'=>'Sports',  'logo'=>'https://fwatch.tv/assets/channel-img/8baa577797_86b00568-9b0a-4972-9692-d4dd286a7f55.jpeg', 'url'=>'proxy.php?proxy_url=' . urlencode(base64_encode('https://andro.evrenesoglu57.click/checklist/androstreamliveexn3.m3u8')) . '&m3u8=1'],
];

$catCounts = [];
foreach ($channels as $ch) {
    $catCounts[$ch['cat']] = ($catCounts[$ch['cat']] ?? 0) + 1;
}

header('Content-Type: text/html; charset=utf-8');
?>
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>FLIX — Live TV</title>
<meta name="description" content="Watch 100+ live TV channels — Bangla, Hindi, English, Sports, Kids">
<meta name="theme-color" content="#0a0a0c">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
<style>
/* ═══════════════════════════════════════════
   RESET & DESIGN TOKENS
═══════════════════════════════════════════ */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  /* Premium dark palette */
  --bg:        #0a0a0c;
  --bg-2:      #0e0e12;
  --surface:   #141418;
  --card:      #16161c;
  --hover:     #202028;
  --border:    rgba(255,255,255,.07);
  --border-2:  rgba(255,255,255,.12);
  --accent:    #ff2d2d;
  --accent-2:  #ff5a3c;
  --accent-glow: rgba(255,45,45,.35);
  --text:      #f4f4f6;
  --muted:     #b6b6c2;
  --dim:       #7a7a88;
  --glass:     rgba(18,18,24,.72);
  --radius:    14px;
  --sidebar-w: 248px;
  --header-h:  60px;
  --shadow:    0 10px 40px rgba(0,0,0,.55);
}

html { scroll-behavior: smooth; -webkit-text-size-adjust: 100%; overflow-x: hidden; }
body {
  font-family: 'Inter', system-ui, sans-serif;
  background:
    radial-gradient(1200px 600px at 80% -10%, rgba(255,45,45,.08), transparent 60%),
    radial-gradient(900px 500px at -10% 10%, rgba(80,120,255,.05), transparent 55%),
    var(--bg);
  color: var(--text);
  min-height: 100vh;
  max-width: 100%;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}
button { cursor: pointer; font-family: inherit; color: inherit; }
img { display: block; }
svg { display: block; }
::selection { background: var(--accent-glow); }

/* Custom scrollbar */
::-webkit-scrollbar { width: 10px; height: 10px; }
::-webkit-scrollbar-thumb { background: #2a2a33; border-radius: 10px; border: 2px solid var(--bg); }
::-webkit-scrollbar-thumb:hover { background: #3a3a45; }

/* ═══════════════════════════════════════════
   HEADER (glassmorphism)
═══════════════════════════════════════════ */
#header {
  position: fixed; top: 0; left: 0; right: 0;
  height: var(--header-h);
  background: var(--glass);
  backdrop-filter: blur(18px) saturate(160%);
  -webkit-backdrop-filter: blur(18px) saturate(160%);
  border-bottom: 1px solid var(--border);
  z-index: 300;
  display: flex; align-items: center;
  padding: 0 18px; gap: 10px;
}
.hd-left { display: flex; align-items: center; gap: 12px; flex-shrink: 0; }
#menuBtn {
  background: none; border: none; color: var(--text);
  width: 42px; height: 42px; border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  transition: background .18s, transform .18s;
}
#menuBtn:hover { background: var(--hover); }
#menuBtn:active { transform: scale(.92); }
/* Logo */
.logo-wrap {
  display: flex; align-items: center; gap: 9px;
  text-decoration: none; color: var(--text);
  font-size: 1.18rem; font-weight: 800; letter-spacing: -.6px;
}
.logo-icon {
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  border-radius: 9px;
  width: 30px; height: 30px;
  display: flex; align-items: center; justify-content: center;
  color: #fff; box-shadow: 0 4px 14px var(--accent-glow);
  font-size: .65rem; font-weight: 900; letter-spacing: .5px;
}
.logo-icon svg { width: 16px; height: 16px; }

/* Search */
.search-form { flex: 1; max-width: 560px; margin: 0 auto; display: flex; }
.search-input-wrap {
  flex: 1; display: flex; align-items: center; gap: 8px;
  border: 1px solid var(--border-2);
  border-radius: 30px;
  padding: 0 16px;
  background: rgba(255,255,255,.04);
  transition: border-color .18s, box-shadow .18s, background .18s;
}
.search-input-wrap:focus-within {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-glow);
  background: rgba(255,255,255,.06);
}
.search-input-wrap svg { width: 18px; height: 18px; color: var(--dim); flex-shrink: 0; }
.search-input-wrap input {
  flex: 1; background: none; border: none; outline: none;
  color: var(--text); font-size: .95rem; padding: 11px 0; font-family: inherit;
}
.search-input-wrap input::placeholder { color: var(--dim); }

.hd-right { display: flex; align-items: center; gap: 8px; margin-left: auto; flex-shrink: 0; }
.live-pill {
  display: flex; align-items: center; gap: 6px;
  background: rgba(255,45,45,.12); border: 1px solid rgba(255,45,45,.3);
  border-radius: 30px; padding: 6px 13px;
  font-size: .74rem; font-weight: 700; color: var(--accent);
  letter-spacing: .5px;
}
.live-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--accent);
  box-shadow: 0 0 8px var(--accent); animation: blink 1.4s ease infinite; }
@keyframes blink { 0%,100%{opacity:1}50%{opacity:.25} }

/* ═══════════════════════════════════════════
   SIDEBAR (glass)
═══════════════════════════════════════════ */
#sidebar {
  position: fixed; top: var(--header-h); left: 0; bottom: 0;
  width: var(--sidebar-w);
  background: var(--bg-2);
  border-right: 1px solid var(--border);
  overflow-y: auto; overflow-x: hidden;
  -webkit-overflow-scrolling: touch;
  z-index: 200;
  transform: translateX(0);
  transition: width .22s ease, transform .22s ease;
  padding: 14px 0;
}
#sidebar.collapsed { width: 76px; }
#sidebar::-webkit-scrollbar { width: 0; }

.sb-section { padding: 0 0 12px; border-bottom: 1px solid var(--border); margin-bottom: 12px; }
.sb-section:last-child { border-bottom: none; }
.sb-label {
  font-size: .66rem; font-weight: 700; color: var(--dim);
  letter-spacing: 1px; text-transform: uppercase;
  padding: 8px 26px 6px; transition: opacity .2s;
}
#sidebar.collapsed .sb-label { opacity: 0; pointer-events: none; }

.sb-item {
  display: flex; align-items: center; gap: 16px;
  padding: 11px 22px; border-radius: 12px; margin: 2px 10px;
  color: var(--muted);
  font-size: .9rem; font-weight: 600;
  cursor: pointer; position: relative;
  transition: background .16s, color .16s;
  white-space: nowrap; overflow: hidden;
}
.sb-item:hover { background: var(--hover); color: var(--text); }
.sb-item.active { background: rgba(255,45,45,.12); color: #fff; }
.sb-item.active::before {
  content: ''; position: absolute; left: 0; top: 18%; bottom: 18%;
  width: 3px; border-radius: 3px; background: var(--accent);
}
.sb-icon { flex-shrink: 0; display: flex; }
.sb-icon svg { width: 22px; height: 22px; }
.sb-text { transition: opacity .2s; }
#sidebar.collapsed .sb-text { opacity: 0; }
.sb-count {
  margin-left: auto; font-size: .68rem; font-weight: 700;
  background: var(--border-2); border-radius: 8px; padding: 2px 8px;
  color: var(--muted); transition: opacity .2s;
}
#sidebar.collapsed .sb-count { opacity: 0; }

#sidebarOverlay {
  display: none; position: fixed; inset: 0;
  background: rgba(0,0,0,.6); backdrop-filter: blur(2px); z-index: 190;
}

/* ═══════════════════════════════════════════
   MAIN
═══════════════════════════════════════════ */
#mainWrap {
  margin-top: var(--header-h);
  margin-left: var(--sidebar-w);
  transition: margin-left .22s;
  min-height: calc(100vh - var(--header-h));
}
#mainWrap.full { margin-left: 76px; }

/* Category chips */
.chips-wrap {
  position: sticky; top: var(--header-h); z-index: 100;
  background: var(--glass);
  backdrop-filter: blur(16px) saturate(160%);
  -webkit-backdrop-filter: blur(16px) saturate(160%);
  border-bottom: 1px solid var(--border);
  padding: 12px 24px; display: flex; gap: 10px; overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}
.chips-wrap::-webkit-scrollbar { height: 0; }
.chip {
  display: inline-flex; align-items: center; gap: 7px;
  background: rgba(255,255,255,.05); border: 1px solid var(--border);
  border-radius: 30px; color: var(--text);
  font-size: .82rem; font-weight: 600;
  padding: 8px 15px; white-space: nowrap;
  transition: background .16s, color .16s, border-color .16s, transform .12s;
}
.chip svg { width: 16px; height: 16px; }
.chip:hover { background: var(--hover); border-color: var(--border-2); }
.chip:active { transform: scale(.96); }
.chip.active {
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  color: #fff; border-color: transparent;
  box-shadow: 0 4px 16px var(--accent-glow);
}

.grid-container { padding: 22px 24px 48px; }
.grid-title { font-size: 1.05rem; font-weight: 800; margin-bottom: 18px; letter-spacing: -.3px; }

.channel-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));
  gap: 18px;
}

/* Channel card */
.ch-card { cursor: pointer; border-radius: var(--radius); transition: transform .2s; position: relative; }
.ch-card:hover { transform: translateY(-4px); }
.ch-card:hover .ch-thumb-inner { transform: scale(1.05); }
/* Active / currently-playing card */
.ch-card.playing .ch-thumb { border-color: var(--accent); box-shadow: 0 0 0 2px var(--accent), 0 8px 28px var(--accent-glow); }
.ch-card.playing .ch-name { color: var(--accent); }
.now-badge {
  position: absolute; top: 9px; right: 9px; z-index: 3;
  display: inline-flex; align-items: center; gap: 5px;
  background: var(--accent); color: #fff;
  font-size: .6rem; font-weight: 800; letter-spacing: .4px;
  border-radius: 6px; padding: 3px 7px; box-shadow: 0 2px 8px var(--accent-glow);
}
.now-badge svg { width: 11px; height: 11px; }

.ch-thumb {
  aspect-ratio: 16/9;
  background: linear-gradient(160deg, #1b1b22, #121217);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden; position: relative;
}
.ch-thumb-inner {
  width: 100%; height: 100%;
  display: flex; align-items: center; justify-content: center;
  padding: 18px; transition: transform .28s;
}
.ch-thumb-inner img { max-width: 100%; max-height: 100%; object-fit: contain;
  filter: drop-shadow(0 4px 12px rgba(0,0,0,.6)); }

/* Broken/missing logo placeholder */
.logo-ph {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: 8px; width: 100%; height: 100%;
  color: var(--dim);
}
.logo-ph svg { width: 38%; height: 38%; max-width: 56px; opacity: .55; }
.logo-ph .ph-txt {
  font-size: .68rem; font-weight: 700; letter-spacing: .3px;
  color: var(--dim); text-align: center; padding: 0 6px;
  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
}
.ch-avatar .logo-ph svg, .nav-ch-thumb .logo-ph svg,
.un-thumb .logo-ph svg, .watch-ch-avatar .logo-ph svg { width: 60%; height: 60%; }
.ch-avatar .logo-ph .ph-txt, .nav-ch-thumb .logo-ph .ph-txt,
.un-thumb .logo-ph .ph-txt, .watch-ch-avatar .logo-ph .ph-txt { display: none; }

.live-tag {
  position: absolute; bottom: 9px; left: 9px;
  display: inline-flex; align-items: center; gap: 5px;
  background: var(--accent); color: #fff;
  font-size: .62rem; font-weight: 800;
  border-radius: 6px; padding: 3px 7px; letter-spacing: .5px;
  box-shadow: 0 2px 8px var(--accent-glow);
}
.live-tag .live-dot { background: #fff; box-shadow: none; width: 5px; height: 5px; }
.ch-hover-play {
  position: absolute; inset: 0; background: rgba(0,0,0,.45);
  display: flex; align-items: center; justify-content: center;
  opacity: 0; transition: opacity .2s; border-radius: var(--radius);
}
.ch-card:hover .ch-hover-play { opacity: 1; }
.ch-play-circle {
  width: 54px; height: 54px; border-radius: 50%;
  background: rgba(255,45,45,.85); backdrop-filter: blur(4px);
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 6px 20px var(--accent-glow);
}
.ch-play-circle svg { width: 22px; height: 22px; color: #fff; }

.ch-meta { padding: 12px 4px 4px; display: flex; gap: 11px; }
.ch-avatar {
  width: 38px; height: 38px; border-radius: 50%;
  background: var(--card); border: 1px solid var(--border);
  flex-shrink: 0; overflow: hidden;
  display: flex; align-items: center; justify-content: center; padding: 5px;
}
.ch-avatar img { width: 100%; height: 100%; object-fit: contain; }
.ch-info { flex: 1; min-width: 0; }
.ch-name {
  font-size: .9rem; font-weight: 700; line-height: 1.3;
  overflow: hidden; text-overflow: ellipsis;
  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;
}
.ch-sub { font-size: .77rem; color: var(--dim); margin-top: 3px; font-weight: 500; }

/* ═══════════════════════════════════════════
   WATCH VIEW
═══════════════════════════════════════════ */
#watchView { display: none; padding: 18px 20px 32px; }
#watchView.visible { display: flex; gap: 26px; align-items: flex-start; }
.watch-primary { flex: 1; min-width: 0; }

.player-box {
  background: #000; border-radius: var(--radius);
  overflow: hidden; position: relative;
  box-shadow: var(--shadow);
  border: 1px solid var(--border);
}
.player-box video { width: 100%; aspect-ratio: 16/9; display: block; background: #000; }
.player-loader {
  position: absolute; inset: 0;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  background: rgba(0,0,0,.65); gap: 14px; pointer-events: none; text-align: center; padding: 16px;
}
.player-spinner {
  width: 52px; height: 52px;
  border: 3px solid rgba(255,255,255,.12);
  border-top-color: var(--accent); border-radius: 50%;
  animation: spin .8s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }
.player-loader-txt { font-size: .88rem; color: rgba(255,255,255,.7); font-weight: 500; }
.player-loader.hide { display: none; }

/* Prev/Next overlay buttons */
.video-nav-btn {
  position: absolute; top: 50%; transform: translateY(-50%);
  background: rgba(0,0,0,.5); backdrop-filter: blur(6px);
  border: 1px solid rgba(255,255,255,.12); color: #fff;
  width: 50px; height: 68px; border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  opacity: 0; transition: opacity .2s, background .2s; z-index: 10;
}
.video-nav-btn svg { width: 26px; height: 26px; }
.video-nav-btn:hover { background: rgba(255,45,45,.85); }
.player-box:hover .video-nav-btn { opacity: 1; }
#prevVideoBtn { left: 10px; }
#nextVideoBtn { right: 10px; }

/* Title row */
.watch-title-row { margin-top: 12px; display: flex; align-items: center; gap: 12px; }
.watch-ch-avatar {
  width: 40px; height: 40px; border-radius: 50%;
  background: var(--card); border: 1px solid var(--border);
  flex-shrink: 0; overflow: hidden; padding: 5px;
  display: flex; align-items: center; justify-content: center;
}
.watch-ch-avatar img { width: 100%; height: 100%; object-fit: contain; }
.watch-title-info { flex: 1; min-width: 0; display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
.watch-ch-name { font-size: 1rem; font-weight: 800; line-height: 1.3; letter-spacing: -.2px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.watch-ch-meta { font-size: .78rem; color: var(--dim); font-weight: 500; white-space: nowrap; }
.watch-live-badge {
  display: inline-flex; align-items: center; gap: 5px;
  background: var(--accent); color: #fff;
  border-radius: 6px; padding: 2px 8px;
  font-size: .66rem; font-weight: 800;
}

/* Nav buttons — always side by side */
.nav-row { display: flex; gap: 10px; margin-top: 12px; }
.nav-ch-btn {
  flex: 1; background: var(--card); border: 1px solid var(--border);
  border-radius: 12px; padding: 10px 12px;
  font-size: .82rem; font-weight: 600;
  display: flex; align-items: center; gap: 10px;
  transition: background .16s, border-color .16s, transform .12s; overflow: hidden;
  min-width: 0;
}
.nav-ch-btn:hover { background: var(--hover); border-color: var(--border-2); }
.nav-ch-btn:active { transform: scale(.98); }
.nav-ch-btn.next-btn { flex-direction: row-reverse; text-align: right; }
.nav-ch-thumb {
  width: 44px; height: 28px; border-radius: 7px;
  background: #0f0f13; flex-shrink: 0; overflow: hidden;
  display: flex; align-items: center; justify-content: center; padding: 4px;
}
.nav-ch-thumb img { max-width: 100%; max-height: 100%; object-fit: contain; }
.nav-ch-info { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 2px; }
.nav-ch-btn.next-btn .nav-ch-label { justify-content: flex-end; }
.nav-ch-label {
  font-size: .62rem; color: var(--dim); font-weight: 700;
  display: flex; align-items: center; gap: 4px; text-transform: uppercase; letter-spacing: .5px;
}
.nav-ch-label svg { width: 12px; height: 12px; }
.nav-ch-name { font-size: .8rem; font-weight: 700; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

/* Secondary (Channel list) — height-locked, scrolls independently */
.watch-secondary {
  width: 380px; flex-shrink: 0;
  /* Sticky + fixed height so the page/player never scroll with the list */
  position: sticky;
  top: calc(var(--header-h) + 22px);
  align-self: flex-start;
  height: calc(100vh - var(--header-h) - 44px);
  display: flex; flex-direction: column;
  background: var(--bg-2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}
.up-next-header {
  display: flex; align-items: center; gap: 9px;
  padding: 15px 16px; margin: 0; flex-shrink: 0;
  border-bottom: 1px solid var(--border);
}
.up-next-header svg { width: 18px; height: 18px; color: var(--accent); }
.up-next-title { font-size: .95rem; font-weight: 700; letter-spacing: -.2px; }
.up-next-count {
  margin-left: auto; font-size: .68rem; font-weight: 700;
  color: var(--muted); background: var(--hover);
  border-radius: 20px; padding: 3px 10px;
}

/* The ONLY element that scrolls vertically */
.up-next-list {
  flex: 1; min-height: 0;
  overflow-y: auto; overscroll-behavior: contain;
  -webkit-overflow-scrolling: touch;
  display: flex; flex-direction: column; gap: 4px;
  padding: 8px;
}
.up-next-list::-webkit-scrollbar { width: 8px; }
.up-next-list::-webkit-scrollbar-thumb { background: #2a2a33; border-radius: 8px; border: 2px solid var(--bg-2); }

.up-next-item {
  display: flex; gap: 10px; align-items: center; cursor: pointer;
  border-radius: 9px; padding: 7px 10px;
  transition: background .15s; border: 1px solid transparent;
}
.up-next-item:hover { background: var(--hover); }
.up-next-item.playing { background: rgba(255,45,45,.1); border-color: rgba(255,45,45,.25); }
.un-thumb {
  width: 68px; height: 42px; border-radius: 8px;
  background: linear-gradient(160deg, #1b1b22, #121217);
  flex-shrink: 0; overflow: hidden;
  display: flex; align-items: center; justify-content: center; padding: 6px;
  position: relative;
}
.un-thumb img { max-width: 100%; max-height: 100%; object-fit: contain; }
.un-live-tag {
  position: absolute; bottom: 3px; left: 3px;
  background: var(--accent); color: #fff;
  font-size: .5rem; font-weight: 800;
  border-radius: 3px; padding: 1px 3px;
}
.un-info { flex: 1; min-width: 0; }
.un-name {
  font-size: .82rem; font-weight: 600; line-height: 1.25;
  overflow: hidden; text-overflow: ellipsis;
  display: -webkit-box; -webkit-line-clamp: 1; -webkit-box-orient: vertical;
}
.un-cat { font-size: .68rem; color: var(--dim); margin-top: 2px; font-weight: 500; }
.un-playing-icon {
  font-size: .66rem; color: var(--accent); margin-top: 3px; font-weight: 700;
  display: flex; align-items: center; gap: 4px;
}
.un-playing-icon svg { width: 11px; height: 11px; }

/* Keyboard shortcuts — desktop only, minimal */
.shortcuts-hint { display: none; }


/* ═══════════════════════════════════════════
   TOAST
═══════════════════════════════════════════ */
#toast {
  position: fixed; bottom: 26px; left: 50%;
  transform: translateX(-50%) translateY(70px);
  background: var(--surface); color: var(--text);
  border: 1px solid var(--border-2);
  border-radius: 12px; padding: 12px 22px;
  font-size: .86rem; font-weight: 600;
  z-index: 999; transition: transform .3s, opacity .3s;
  opacity: 0; pointer-events: none; white-space: nowrap;
  box-shadow: var(--shadow);
}
#toast.show { transform: translateX(-50%) translateY(0); opacity: 1; }

/* ═══════════════════════════════════════════
   RESPONSIVE
═══════════════════════════════════════════ */
@media (max-width: 1100px) {
  :root { --sidebar-w: 76px; }
  #sidebar { width: 76px; }
  #sidebar .sb-text, #sidebar .sb-label, #sidebar .sb-count { opacity: 0; pointer-events: none; }
  #mainWrap { margin-left: 76px; }
  .watch-secondary { width: 320px; }
}
@media (max-width: 900px) {
  :root { --sidebar-w: 0px; }
  #sidebar { transform: translateX(-248px); width: 248px; }
  #sidebar.mobile-open { transform: translateX(0); box-shadow: var(--shadow); }
  #sidebar .sb-text, #sidebar .sb-label, #sidebar .sb-count { opacity: 1; pointer-events: all; }
  #mainWrap { margin-left: 0; }
  #sidebarOverlay { display: block; opacity: 0; pointer-events: none; transition: opacity .2s; }
  #sidebarOverlay.show { opacity: 1; pointer-events: all; }

  /* Collapse mainWrap to content height — watch-secondary is fixed so no phantom gap */
  #mainWrap { min-height: unset; }

  /* Watch view: single column, no excess space */
  #watchView { padding: 12px 14px 24px; }
  #watchView.visible { flex-direction: column; gap: 0; }

  /* Channel panel: inline below nav buttons, scrollable */
  .watch-secondary {
    position: static;
    width: 100%;
    height: auto;
    max-height: 55vh;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    margin-top: 14px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transform: none !important;
    transition: none;
  }

  /* Nav row always side by side */
  .nav-row { flex-direction: row !important; gap: 8px; margin-top: 10px; }
  .nav-ch-btn { padding: 9px 10px; gap: 8px; }
  .nav-ch-thumb { width: 40px; height: 26px; }

  /* Compact channel list items */
  .up-next-list { padding: 6px; gap: 2px; }
  .up-next-item { padding: 6px 8px; border-radius: 8px; }
  .un-thumb { width: 60px; height: 38px; }

  .channel-grid { grid-template-columns: repeat(auto-fill, minmax(150px,1fr)); }
  .shortcuts-hint { display: none !important; }
  .video-nav-btn { display: none !important; }
  .up-next-header { padding: 10px 14px; }
}

@media (max-width: 768px) {
  #searchToggleBtn { display: flex !important; }
  .search-form {
    display: none;
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    background: var(--bg-2);
    z-index: 310;
    max-width: none;
    padding: 0 14px;
    align-items: center;
  }
  .search-form.active { display: flex; }
  .search-form.active #closeSearchBtn { display: flex !important; }
  .search-form.active .search-input-wrap { width: 100%; border-color: var(--accent); }
}

@media (max-width: 480px) {
  :root { --header-h: 52px; }
  .channel-grid { grid-template-columns: repeat(2,1fr); gap: 10px; }
  #header { padding: 0 10px; gap: 6px; }
  .logo-wrap span, .logo-wrap { font-size: 1rem; }
  .live-pill { display: none; }
  .search-form { max-width: none; }
  .grid-container { padding: 12px; }
  #watchView { padding: 8px 10px 20px; }
  .nav-ch-name { font-size: .75rem; }
  .nav-ch-thumb { width: 36px; height: 24px; }
}
/* Touch targets */
@media (hover: none) {
  .video-nav-btn { opacity: 1; }
  .ch-hover-play { opacity: 0; }
}

.ch-thumb-inner{
    width:100%;
    height:100%;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:18px;
    background:#fff;
    border-radius:10px;
}

.ch-avatar,
.watch-ch-avatar,
.nav-ch-thumb,
.un-thumb{
    background:#fff;
}

/* ═══════════════════════════════════════════
   SITE PRELOADER
   ═══════════════════════════════════════════ */
#sitePreloader {
  position: fixed;
  inset: 0;
  background: #0a0a0c;
  z-index: 99999;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: opacity 0.4s ease, transform 0.4s ease;
}
#sitePreloader.fade-out {
  opacity: 0;
  pointer-events: none;
  transform: scale(1.05);
}
.preloader-content {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
}
.preloader-logo {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 2.2rem;
  font-weight: 800;
  letter-spacing: -1px;
  color: var(--text);
}
.preloader-logo span {
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}
.preloader-spinner-bar {
  width: 180px;
  height: 3px;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 4px;
  overflow: hidden;
}
.preloader-progress {
  width: 40%;
  height: 100%;
  background: linear-gradient(90deg, var(--accent), var(--accent-2));
  border-radius: 4px;
  animation: loading-bar 1.5s infinite ease-in-out;
}
@keyframes loading-bar {
  0% { transform: translateX(-100%); }
  50% { transform: translateX(150%); }
  100% { transform: translateX(-100%); }
}
.preloader-status {
  font-size: 0.85rem;
  color: var(--dim);
  font-weight: 500;
  letter-spacing: 0.5px;
}
</style>
</head>
<body>

<!-- ═══ PRELOADER ═══ -->
<div id="sitePreloader">
  <div class="preloader-content">
    <div class="preloader-logo">
      <div class="logo-icon" style="background: linear-gradient(135deg, var(--accent), var(--accent-2)); border-radius: 9px; width: 30px; height: 30px; display: flex; align-items: center; justify-content: center; color: #fff; box-shadow: 0 4px 14px var(--accent-glow);">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width:16px;height:16px;"><path d="M5 12.55a11 11 0 0 1 14.08 0"/><path d="M1.42 9a16 16 0 0 1 21.16 0"/><path d="M8.53 16.11a6 6 0 0 1 6.95 0"/><circle cx="12" cy="20" r="1" fill="currentColor" stroke="none"/></svg>
      </div>
      FLIX<span>TV</span>
    </div>
    <div class="preloader-spinner-bar">
      <div class="preloader-progress"></div>
    </div>
    <div class="preloader-status" id="preloaderStatus">Checking server connection...</div>
  </div>
</div>


<!-- ═══ HEADER ═══ -->
<header id="header">
  <div class="hd-left">
    <button id="menuBtn" title="Menu" aria-label="Toggle menu">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
    </button>
    <a class="logo-wrap" href="#" id="homeLink">
      <div class="logo-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12.55a11 11 0 0 1 14.08 0"/><path d="M1.42 9a16 16 0 0 1 21.16 0"/><path d="M8.53 16.11a6 6 0 0 1 6.95 0"/><circle cx="12" cy="20" r="1" fill="currentColor" stroke="none"/></svg>
      </div>
      FLIX<span style="background:linear-gradient(135deg,var(--accent),var(--accent-2));-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent">TV</span>
    </a>
  </div>

  <form class="search-form" id="searchForm" onsubmit="return false">
    <div class="search-input-wrap">
      <button type="button" id="closeSearchBtn" aria-label="Close search" style="background:none;border:none;color:var(--dim);display:none;align-items:center;justify-content:center;margin-right:4px;cursor:pointer;">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width:20px;height:20px;"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
      </button>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="search-icon-svg"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
      <input type="text" id="searchInput" placeholder="Search..." autocomplete="off" enterkeyhint="search">
    </div>
  </form>

  <div class="hd-right">
    <button id="searchToggleBtn" aria-label="Open search" style="background:none;border:none;color:var(--text);width:42px;height:42px;border-radius:12px;display:none;align-items:center;justify-content:center;transition:background .18s;cursor:pointer;margin-right:4px;">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:20px;height:20px;"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
    </button>
    <div class="live-pill"><div class="live-dot"></div> LIVE</div>
  </div>
</header>

<!-- ═══ SIDEBAR OVERLAY (mobile) ═══ -->
<div id="sidebarOverlay"></div>

<!-- ═══ SIDEBAR ═══ -->
<nav id="sidebar">
  <div class="sb-section">
    <div class="sb-item active" data-cat="All" id="sidebarHome">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><path d="M9 22V12h6v10"/></svg></span>
      <span class="sb-text">Home</span>
    </div>
  </div>
  <div class="sb-section">
    <div class="sb-label">Categories</div>
    <div class="sb-item" data-cat="Sports">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9H4.5a2.5 2.5 0 0 1 0-5H6"/><path d="M18 9h1.5a2.5 2.5 0 0 0 0-5H18"/><path d="M4 22h16"/><path d="M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22"/><path d="M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22"/><path d="M18 2H6v7a6 6 0 0 0 12 0V2Z"/></svg></span>
      <span class="sb-text">Sports</span>
      <span class="sb-count"><?= isset($catCounts['Sports']) ? $catCounts['Sports'] : 0 ?></span>
    </div>
    <div class="sb-item" data-cat="Bangla">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="15" x="2" y="7" rx="2"/><polyline points="17 2 12 7 7 2"/></svg></span>
      <span class="sb-text">Bangla</span>
      <span class="sb-count"><?= isset($catCounts['Bangla']) ? $catCounts['Bangla'] : 0 ?></span>
    </div>
    <div class="sb-item" data-cat="Hindi">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.2 6 3 11l-.9-2.4c-.3-1.1.3-2.2 1.3-2.5l13.5-4c1.1-.3 2.2.3 2.5 1.3Z"/><path d="m6.2 5.3 3.1 3.9"/><path d="m12.4 3.4 3.1 4"/><path d="M3 11h18v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z"/></svg></span>
      <span class="sb-text">Hindi</span>
      <span class="sb-count"><?= isset($catCounts['Hindi']) ? $catCounts['Hindi'] : 0 ?></span>
    </div>
    <div class="sb-item" data-cat="English">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/></svg></span>
      <span class="sb-text">English</span>
      <span class="sb-count"><?= isset($catCounts['English']) ? $catCounts['English'] : 0 ?></span>
    </div>
    <div class="sb-item" data-cat="Kids">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><line x1="9" x2="9.01" y1="9" y2="9"/><line x1="15" x2="15.01" y1="9" y2="9"/></svg></span>
      <span class="sb-text">Kids</span>
      <span class="sb-count"><?= isset($catCounts['Kids']) ? $catCounts['Kids'] : 0 ?></span>
    </div>
    <div class="sb-item" data-cat="Islamic">
      <span class="sb-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/><path d="M19 3v4"/><path d="M21 5h-4"/></svg></span>
      <span class="sb-text">Islamic</span>
      <span class="sb-count"><?= isset($catCounts['Islamic']) ? $catCounts['Islamic'] : 0 ?></span>
    </div>
  </div>
</nav>

<!-- ═══ MAIN WRAP ═══ -->
<div id="mainWrap">

  <!-- HOME VIEW -->
  <div id="homeView">
    <div class="chips-wrap" id="chipsWrap">
      <button class="chip active" data-cat="All">All</button>
      <button class="chip" data-cat="Sports"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9H4.5a2.5 2.5 0 0 1 0-5H6"/><path d="M18 9h1.5a2.5 2.5 0 0 0 0-5H18"/><path d="M4 22h16"/><path d="M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22"/><path d="M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22"/><path d="M18 2H6v7a6 6 0 0 0 12 0V2Z"/></svg>Sports</button>
      <button class="chip" data-cat="Bangla"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="15" x="2" y="7" rx="2"/><polyline points="17 2 12 7 7 2"/></svg>Bangla</button>
      <button class="chip" data-cat="Hindi"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.2 6 3 11l-.9-2.4c-.3-1.1.3-2.2 1.3-2.5l13.5-4c1.1-.3 2.2.3 2.5 1.3Z"/><path d="m6.2 5.3 3.1 3.9"/><path d="m12.4 3.4 3.1 4"/><path d="M3 11h18v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2Z"/></svg>Hindi</button>
      <button class="chip" data-cat="English"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/></svg>English</button>
      <button class="chip" data-cat="Kids"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><line x1="9" x2="9.01" y1="9" y2="9"/><line x1="15" x2="15.01" y1="9" y2="9"/></svg>Kids</button>
      <button class="chip" data-cat="Islamic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/><path d="M19 3v4"/><path d="M21 5h-4"/></svg>Islamic</button>
    </div>
    <div class="grid-container">
      <div class="grid-title" id="gridTitle">All Channels</div>
      <div class="channel-grid" id="channelGrid"></div>
    </div>
  </div>

  <!-- WATCH VIEW -->
  <div id="watchView">
    <!-- Primary -->
    <div class="watch-primary">
      <div class="player-box" id="playerBox">
        <video id="mainVideo" controls playsinline></video>
        <div class="player-loader" id="playerLoader">
          <div class="player-spinner"></div>
          <div class="player-loader-txt">Loading stream...</div>
        </div>
        <button class="video-nav-btn" id="prevVideoBtn" title="Previous channel (←)" aria-label="Previous channel">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
        </button>
        <button class="video-nav-btn" id="nextVideoBtn" title="Next channel (→)" aria-label="Next channel">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
        </button>
      </div>

      <!-- Title row -->
      <div class="watch-title-row" id="watchTitleRow">
        <div class="watch-ch-avatar" id="watchAvatarBox"></div>
        <div class="watch-title-info">
          <div class="watch-ch-name" id="watchChName"></div>
          <div class="watch-ch-meta" id="watchChMeta"></div>
          <span class="watch-live-badge"><span class="live-dot" style="width:5px;height:5px;background:#fff;box-shadow:none"></span> LIVE</span>
        </div>
      </div>

      <!-- Prev/Next nav buttons -->
      <div class="nav-row" id="navRow">
        <button class="nav-ch-btn prev-btn" id="prevBtn">
          <div class="nav-ch-thumb" id="prevThumbBox"></div>
          <div class="nav-ch-info">
            <div class="nav-ch-label"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg> Previous</div>
            <div class="nav-ch-name" id="prevName">—</div>
          </div>
        </button>
        <button class="nav-ch-btn next-btn" id="nextBtn">
          <div class="nav-ch-thumb" id="nextThumbBox"></div>
          <div class="nav-ch-info">
            <div class="nav-ch-label">Next <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg></div>
            <div class="nav-ch-name" id="nextName">—</div>
          </div>
        </button>
      </div><!-- /nav-row -->
    </div><!-- /watch-primary -->

    <!-- Secondary (Channel list) — right sidebar on desktop, inline on mobile -->
    <div class="watch-secondary" id="channelPanel">
      <div class="up-next-header">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><line x1="3" y1="6" x2="3.01" y2="6"/><line x1="3" y1="12" x2="3.01" y2="12"/><line x1="3" y1="18" x2="3.01" y2="18"/></svg>
        <div class="up-next-title">Channels</div>
        <span class="up-next-count" id="upNextCount"></span>
      </div>
      <div class="up-next-list" id="upNextList"></div>
    </div>
  </div>

</div><!-- /mainWrap -->

<div id="toast"></div>

<!-- ═══════════════════════════════════════════
     JAVASCRIPT
═══════════════════════════════════════════ -->
<script>
// ── Data ──────────────────────────────────────
const CHANNELS = <?= json_encode($channels, JSON_UNESCAPED_UNICODE) ?>;
// Local assets
const LOGO = file => `assets/${file}`;

// Inline SVG used as placeholder whenever a logo fails to load or is missing
const ICON_TV = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="15" x="2" y="7" rx="2"/><polyline points="17 2 12 7 7 2"/></svg>';
const ICON_NOWPLAYING = '<svg viewBox="0 0 24 24" fill="currentColor"><polygon points="6 4 20 12 6 20 6 4"/></svg>';

// ── State ─────────────────────────────────────
let currentCat   = 'All';
let currentQuery = '';
let currentIdx   = -1;
let currentPlayingId = null;   // id of the channel currently playing (for active highlight)
let activeProviders = { bdix: true, fwatch: true };
let filteredList = [...CHANNELS];
let hlsMain      = null;
let currentStreamUrl = '';

// ── Helpers ───────────────────────────────────
function esc(s){ return String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }

function showToast(msg, dur=2200) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  clearTimeout(showToast._t);
  showToast._t = setTimeout(() => t.classList.remove('show'), dur);
}

function logoUrl(ch) {
  if (ch.logo && (ch.logo.startsWith('http://') || ch.logo.startsWith('https://'))) {
    return ch.logo;
  }
  return LOGO(ch.logo);
}

// Build a logo <img> that swaps itself for a clean SVG placeholder on error/missing.
// `withName` shows the channel name as a caption inside large thumbnails.
function logoHtml(ch, withName=false) {
  return `<img src="${logoUrl(ch)}" alt="${esc(ch.name)}" loading="lazy"
            data-name="${esc(ch.name)}"${withName ? ' data-name-show="1"' : ''}
            onerror="logoFail(this)">`;
}

// Replace a broken/missing logo <img> with a clean SVG placeholder.
function logoFail(img) {
  const ph = document.createElement('div');
  ph.className = 'logo-ph';
  ph.innerHTML = ICON_TV;
  if (img.dataset.nameShow) {
    const cap = document.createElement('span');
    cap.className = 'ph-txt';
    cap.textContent = img.dataset.name || '';
    ph.appendChild(cap);
  }
  img.replaceWith(ph);
}

function getFiltered() {
  const q = currentQuery.toLowerCase().trim();
  return CHANNELS.filter(c => {
    const isBdix = !isNaN(c.id);
    const isFwatch = String(c.id).startsWith('f');
    if (isBdix && !activeProviders.bdix) return false;
    if (isFwatch && !activeProviders.fwatch) return false;
    
    return (currentCat === 'All' || c.cat === currentCat) &&
           (!q || c.name.toLowerCase().includes(q));
  });
}

// ── Sidebar ───────────────────────────────────
const sidebar  = document.getElementById('sidebar');
const overlay  = document.getElementById('sidebarOverlay');
const mainWrap = document.getElementById('mainWrap');

document.getElementById('menuBtn').addEventListener('click', () => {
  const mobile = window.innerWidth <= 900;
  if (mobile) {
    const open = sidebar.classList.toggle('mobile-open');
    overlay.classList.toggle('show', open);
  } else {
    const collapsed = sidebar.classList.toggle('collapsed');
    mainWrap.classList.toggle('full', collapsed);
  }
});
overlay.addEventListener('click', () => {
  sidebar.classList.remove('mobile-open');
  overlay.classList.remove('show');
});

document.querySelectorAll('.sb-item[data-cat]').forEach(el => {
  el.addEventListener('click', () => {
    selectCat(el.dataset.cat);
    if (window.innerWidth <= 900) {
      sidebar.classList.remove('mobile-open');
      overlay.classList.remove('show');
    }
  });
});

function selectCat(cat) {
  currentCat = cat;
  currentQuery = '';
  document.getElementById('searchInput').value = '';
  document.querySelectorAll('.sb-item[data-cat]').forEach(el =>
    el.classList.toggle('active', el.dataset.cat === cat));
  document.querySelectorAll('.chip[data-cat]').forEach(el =>
    el.classList.toggle('active', el.dataset.cat === cat));
  goHome();
  renderGrid();
}

// ── Chips ─────────────────────────────────────
document.querySelectorAll('.chip[data-cat]').forEach(btn =>
  btn.addEventListener('click', () => selectCat(btn.dataset.cat)));

// ── Search ────────────────────────────────────
document.getElementById('searchInput').addEventListener('input', e => {
  currentQuery = e.target.value;
  if (currentQuery) {
    currentCat = 'All';
    document.querySelectorAll('.chip,.sb-item[data-cat]').forEach(el =>
      el.classList.toggle('active', el.dataset.cat === 'All'));
  }
  goHome();
  renderGrid();
});

// ── Home Link ─────────────────────────────────
document.getElementById('homeLink').addEventListener('click', e => {
  e.preventDefault(); selectCat('All');
});

// ── Render Grid ───────────────────────────────
function renderGrid() {
  filteredList = getFiltered();
  const grid  = document.getElementById('channelGrid');
  const title = document.getElementById('gridTitle');
  const cat   = currentCat === 'All' ? 'All Channels' : currentCat;
  const q     = currentQuery.trim();
  title.textContent = q ? `Search: "${q}" — ${filteredList.length} found`
                        : `${cat} — ${filteredList.length} channels`;
  grid.innerHTML = '';
  filteredList.forEach((ch, idx) => {
    const card = document.createElement('div');
    const isPlaying = ch.id === currentPlayingId;
    card.className = 'ch-card' + (isPlaying ? ' playing' : '');
    card.innerHTML = `
      <div class="ch-thumb">
        <div class="ch-thumb-inner">${logoHtml(ch, true)}</div>
        ${isPlaying ? `<span class="now-badge">${ICON_NOWPLAYING} NOW</span>` : ''}
        <span class="live-tag"><span class="live-dot"></span> LIVE</span>
        <div class="ch-hover-play">
          <div class="ch-play-circle">
            <svg viewBox="0 0 24 24" fill="currentColor"><polygon points="6 4 20 12 6 20 6 4"/></svg>
          </div>
        </div>
      </div>
      <div class="ch-meta">
        <div class="ch-avatar">${logoHtml(ch)}</div>
        <div class="ch-info">
          <div class="ch-name">${esc(ch.name)}</div>
          <div class="ch-sub">${esc(ch.cat)} · Live TV</div>
        </div>
      </div>`;
    card.addEventListener('click', () => openWatch(idx));
    grid.appendChild(card);
  });
}

// ── Navigate to Watch ─────────────────────────
function openWatch(idx) {
  filteredList = getFiltered();
  if (idx < 0 || idx >= filteredList.length) return;
  currentIdx = idx;

  const ch = filteredList[idx];
  showWatchView();
  updateWatchMeta(ch);
  buildUpNext(idx);
  buildNavButtons(idx);
  loadMainStream(ch);
}

function showWatchView() {
  document.getElementById('homeView').style.display = 'none';
  const wv = document.getElementById('watchView');
  wv.style.display = '';
  wv.classList.add('visible');
  window.scrollTo(0,0);
}

function goHome() {
  stopMainStream();
  document.getElementById('homeView').style.display = '';
  const wv = document.getElementById('watchView');
  wv.style.display = 'none';
  wv.classList.remove('visible');
}

function updateWatchMeta(ch) {
  currentPlayingId = ch.id;   // mark active for list highlighting
  document.getElementById('watchAvatarBox').innerHTML = logoHtml(ch);
  document.getElementById('watchChName').textContent = ch.name;
  document.getElementById('watchChMeta').textContent = ch.cat + ' · Live TV';
  document.title = ch.name + ' — FLIX';
}

// ── Stream loading ────────────────────────────
function loadMainStream(ch) {
  const video = document.getElementById('mainVideo');
  const loader = document.getElementById('playerLoader');
  loader.classList.remove('hide');
  loader.querySelector('.player-loader-txt').textContent = 'Loading stream...';
  if (hlsMain) { hlsMain.destroy(); hlsMain = null; }
  video.src = '';
  currentStreamUrl = '';

  if (ch.url) {
    currentStreamUrl = ch.url;
    attachHLS(video, ch.url, () => loader.classList.add('hide'));
    return;
  }

  fetch(`proxy.php?stream=${ch.id}&json=1`)
    .then(r => r.json())
    .then(data => {
      if (!data.url) throw new Error('no url');
      currentStreamUrl = data.url;
      attachHLS(video, data.url, () => loader.classList.add('hide'));
    })
    .catch(() => {
      loader.classList.remove('hide');
      loader.querySelector('.player-loader-txt').textContent = 'Stream unavailable. Use ← / → to try another channel.';
    });
}

function attachHLS(video, url, onReady) {
  if (Hls.isSupported()) {
    const hls = new Hls({ debug: false, enableWorker: true });
    hlsMain = hls;
    hls.loadSource(url);
    hls.attachMedia(video);
    hls.on(Hls.Events.MANIFEST_PARSED, () => {
      video.play().catch(()=>{});
      if (onReady) onReady();
    });
    hls.on(Hls.Events.ERROR, (e, data) => {
      // Show a message only on fatal errors. NEVER auto-advance — stay on channel.
      if (data.fatal) {
        const loader = document.getElementById('playerLoader');
        loader.classList.remove('hide');
        loader.querySelector('.player-loader-txt').textContent = 'Stream error. Use ← / → to try another channel.';
      }
    });
  } else if (video.canPlayType('application/vnd.apple.mpegurl')) {
    video.src = url;
    video.addEventListener('canplay', () => { video.play(); if(onReady)onReady(); }, {once:true});
  }
}

function stopMainStream() {
  const video = document.getElementById('mainVideo');
  if (hlsMain) { hlsMain.destroy(); hlsMain = null; }
  video.src = '';
  document.title = 'FLIX — Live TV';
}

// ── Channel list (with active highlight) ──────
function buildUpNext(currentI) {
  const list = document.getElementById('upNextList');
  list.innerHTML = '';
  const countEl = document.getElementById('upNextCount');
  if (countEl) countEl.textContent = filteredList.length;
  filteredList.forEach((ch, idx) => {
    const isPlaying = idx === currentI;
    const item = document.createElement('div');
    item.className = 'up-next-item' + (isPlaying ? ' playing' : '');
    item.dataset.idx = idx;
    item.innerHTML = `
      <div class="un-thumb">
        ${logoHtml(ch)}
        <span class="un-live-tag">LIVE</span>
      </div>
      <div class="un-info">
        <div class="un-name">${esc(ch.name)}</div>
        <div class="un-cat">${esc(ch.cat)} · Live TV</div>
        ${isPlaying ? `<div class="un-playing-icon">${ICON_NOWPLAYING} Now Playing</div>` : ''}
      </div>`;
    item.addEventListener('click', () => openWatch(idx));
    list.appendChild(item);
  });
  // Scroll the active item into view inside the list
  const active = list.querySelector('.up-next-item.playing');
  if (active) active.scrollIntoView({ block: 'nearest' });
}

// ── Prev/Next buttons ─────────────────────────
function buildNavButtons(idx) {
  const total = filteredList.length;
  const prevCh = filteredList[(idx - 1 + total) % total];
  const nextCh = filteredList[(idx + 1) % total];

  document.getElementById('prevName').textContent = prevCh.name;
  document.getElementById('nextName').textContent = nextCh.name;
  document.getElementById('prevThumbBox').innerHTML = logoHtml(prevCh);
  document.getElementById('nextThumbBox').innerHTML = logoHtml(nextCh);
}

document.getElementById('prevBtn').addEventListener('click', () => navigateChannel(-1));
document.getElementById('nextBtn').addEventListener('click', () => navigateChannel(1));
document.getElementById('prevVideoBtn').addEventListener('click', () => navigateChannel(-1));
document.getElementById('nextVideoBtn').addEventListener('click', () => navigateChannel(1));

// Manual navigation ONLY — user-triggered.
function navigateChannel(dir) {
  const total = filteredList.length;
  if (!total) return;
  currentIdx = ((currentIdx + dir) + total) % total;
  openWatch(currentIdx);
}

// NOTE: No autoplay / auto-next. Player stays on the selected channel.
// 'ended'/'error' events intentionally do NOT switch channels.

// ── Keyboard shortcuts ────────────────────────
document.addEventListener('keydown', e => {
  const watchVisible = document.getElementById('watchView').classList.contains('visible');
  if (document.activeElement.tagName === 'INPUT') return;
  const mainVideo = document.getElementById('mainVideo');

  if (e.key === 'ArrowLeft')  { if(watchVisible){ e.preventDefault(); navigateChannel(-1); } }
  if (e.key === 'ArrowRight') { if(watchVisible){ e.preventDefault(); navigateChannel(1); } }
  if (e.key === 'Escape')     { if(watchVisible){ goHome(); renderGrid(); } }
  if (e.key === ' ') {
    if (watchVisible) { e.preventDefault(); mainVideo.paused ? mainVideo.play() : mainVideo.pause(); }
  }
  if (e.key.toLowerCase() === 'm') {
    if(watchVisible) { mainVideo.muted = !mainVideo.muted; showToast(mainVideo.muted ? 'Muted' : 'Unmuted'); }
  }
});

function updateSidebarCounts() {
  const counts = { All: 0, Sports: 0, Bangla: 0, Hindi: 0, English: 0, Kids: 0, Islamic: 0 };
  
  CHANNELS.forEach(ch => {
    const isBdix = !isNaN(ch.id);
    const isFwatch = String(ch.id).startsWith('f');
    let visible = true;
    if (isBdix && !activeProviders.bdix) visible = false;
    if (isFwatch && !activeProviders.fwatch) visible = false;
    
    if (visible) {
      counts.All++;
      if (counts[ch.cat] !== undefined) {
        counts[ch.cat]++;
      }
    }
  });
  
  document.querySelectorAll('.sb-item[data-cat]').forEach(el => {
    const cat = el.dataset.cat;
    const countEl = el.querySelector('.sb-count');
    if (countEl && counts[cat] !== undefined) {
      countEl.textContent = counts[cat];
    }
  });
}

function hidePreloader() {
  const loader = document.getElementById('sitePreloader');
  if (loader) {
    loader.classList.add('fade-out');
    setTimeout(() => {
      loader.remove(); // Remove completely from DOM
    }, 450);
  }
}

// ── Check Server Speed on site visit ──
(function checkServerSpeed() {
  fetch('proxy.php?check=1')
    .then(r => r.json())
    .then(data => {
      console.log("Preferred server checked:", data.preferred);
      
      let hasBdix = false;
      if (data.providers && data.providers.bdix !== undefined) {
        hasBdix = !!data.providers.bdix;
      } else if (data.latencies) {
        hasBdix = Object.keys(data.latencies).length > 0;
      }
      
      let hasFwatch = true;
      if (data.providers && data.providers.fwatch !== undefined) {
        hasFwatch = !!data.providers.fwatch;
      }
      
      activeProviders.bdix = hasBdix;
      activeProviders.fwatch = hasFwatch;
      
      updateSidebarCounts();
      renderGrid();
      hidePreloader();
    })
    .catch(e => {
      console.warn("Background server check failed:", e);
      // Fail-safe: Assume BDIX is down on hosting if check fails
      activeProviders.bdix = false;
      updateSidebarCounts();
      renderGrid();
      hidePreloader();
    });
})();

// ── URL param: ?ch=ID ─────────────────────────
(function checkUrlParam() {
  const p = new URLSearchParams(location.search);
  const chId = parseInt(p.get('ch'));
  if (chId) {
    filteredList = CHANNELS;
    const idx = filteredList.findIndex(c => c.id === chId);
    if (idx >= 0) { openWatch(idx); return; }
  }
  renderGrid();
})();

// ── Mobile search overlay toggle ───────────────
const searchToggleBtn = document.getElementById('searchToggleBtn');
const closeSearchBtn = document.getElementById('closeSearchBtn');
const searchForm = document.getElementById('searchForm');
const searchInput = document.getElementById('searchInput');

if (searchToggleBtn && searchForm) {
  searchToggleBtn.addEventListener('click', () => {
    searchForm.classList.add('active');
    searchInput.focus();
  });
}
if (closeSearchBtn && searchForm) {
  closeSearchBtn.addEventListener('click', () => {
    searchForm.classList.remove('active');
    searchInput.value = '';
    searchInput.dispatchEvent(new Event('input'));
  });
}
</script>
</body>
</html>
